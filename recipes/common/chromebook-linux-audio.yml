modules:
  - type: script
    snippets:
      - "git clone https://github.com/mikaelvz/chromebook-linux-audio.git /tmp/chromebook-linux-audio"
      - "cd /tmp/chromebook-linux-audio && ./setup-audio --platform cml"